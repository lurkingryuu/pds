#include <stdio.h>
#include <stdlib.h>


int main(){
    FILE* fp;
    int n, m;

    if ((fp = fopen("matrix.txt", "r")) == NULL){
        printf("Cannot open the file!\n");
        return 0;
    }
    
    fscanf

    return 0;
}